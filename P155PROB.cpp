#include<iostream>
using namespace std;
int main(){
	int x, s, a, b, c, d, e;
	cin >> x >> s >> a >> b >> c >> d >> e;
	cout << -x*s + a << " " << -x*s + b << " "<<-x*s + c << " " << -x*s + d <<" " << -x *s +e;
	return 0;
}
 
